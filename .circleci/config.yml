version: 2
jobs:
    build:
        parallelism: 4
        docker:
            - image: cimg/base:2022.09
        steps:
            - checkout

            - setup_remote_docker

            - run:
                  name: Run docker-compose publish
                  command: |
                      docker-compose --file docker-compose.publish.yml build
                      docker-compose --file docker-compose.publish.yml run sut
